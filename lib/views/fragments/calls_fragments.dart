import 'package:flutter/cupertino.dart';

class CallFragment extends StatelessWidget {
  const CallFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Call Fragment"));
  }
}
